# Publish directory must stay unset here and in the Netlify UI (blank).
# @netlify/plugin-nextjs sets the correct output; repo root as publish breaks the build.
[build]
  command = "npm run build"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "20"
